[project]
name = "agent-service"
requires-python = ">=3.11"
dependencies = [
  "tree-sitter>=0.20",
  "zod-py>=1.0",
]

[project.scripts]
graft = "graft.cli:main"

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]

[[tool.graft.targets]]
name = "core"
path = "src"
