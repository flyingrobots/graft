{
  "name": "@example/agent-service",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "lint": "eslint ."
  },
  "dependencies": {
    "zod": "^4.0.0",
    "web-tree-sitter": "^0.20.8"
  },
  "devDependencies": {
    "typescript": "^6.0.2",
    "vitest": "^4.1.2"
  },
  "workspaces": [
    "packages/*"
  ],
  "compilerOptions": {
    "strict": true,
    "module": "NodeNext"
  }
}
