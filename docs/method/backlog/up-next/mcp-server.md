# MCP Server Transport

Expose all Phase 1 commands as MCP tools. The MCP server is the
cross-LLM transport — any agent that speaks MCP can use Graft.

Depends on: Phase 1 command implementations being stable.

Effort: L
